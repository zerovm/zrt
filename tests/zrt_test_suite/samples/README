This directory contains some functional tests for ZRT library

command_line/
  demonstrates argc/argv passed via manifest, in case of unexpected parameters it generates assertion.

environment/
  all types of access to environment variables, in case of unexpected variables values it generates assertion.

file_stat/
  demonstrates stat() / fstat() for various channels. Also it uses assertions to test expecting stat data; 
  
manifest_test/
  simple functional test that tests using assertions for channels data, expected that it's parsing correctly. 
  
bigfile/
  It creates 5GB file, in case of error it generates assertion.

seek/
  It tests seeking for random write-only files.
  