--------------------------------------------
Networking Library
--------------------------------------------

1. Networking Library is provide access to simple channels configuration related to distributed applications, 
that is based on channel aliases configured once in manifest file and accessible runtime via readir;
Using of networking library allow to setup networking channels set just use dynamical 
channel aliases from configuration, that way eliminates most of hardcoded data;
2. In order link that static library add the folowing flags: -L{YOUR_ZRT_ROOT}/lib -lzrt -lnetworking;
